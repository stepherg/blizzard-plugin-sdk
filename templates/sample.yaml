plugin:
  name: Demo
  description: Demo rbus method plugin
  permissions: []

methods:
  - name: "Echo()"
    permissions: []
    parameters_schema:
      kind: object
      object:
        properties:
          input:
            kind: basic
            basic: string
    result_schema:
      kind: object
      object:
        properties:
          response:
            kind: basic
            basic: string
