plugin:
  name: Demo
  description: Demo rbus method plugin
  permissions: []

methods:
  - name: "Device.Echo()"
    permissions: []
    parameters_schema:
      kind: object
      object:
        properties:
          params:
            kind: basic
            basic: string
    result_schema:
      kind: object
      object:
        properties:
          response:
            kind: basic
            basic: string
